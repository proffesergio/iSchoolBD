# PROJECT_CONTEXT (auto-loaded by LLMs)

- Root: `C:\Users\bhnbi\Music\iSchool` — npm workspaces `apps/*`, `packages/*`.
- PWA entry: `apps/web/app/page.tsx` — Sworoborna grid + fullscreen modal + XP.
- API entry: `apps/api/src/main.ts` → `app.module.ts` → learn/progress/health.
- Engine: `packages/ai-engine/src/{sworoborna,validator,tokenOptimizer,mockEngine}.js`.
- Schema: `packages/ai-engine/ischool.schema.json`.
- DB: `supabase/schema.sql`.
- CI: `.github/workflows/ci.yml` (engine → api → web).
- Ask clarifying questions when tier/content/auth scope is ambiguous. Keep token-efficient Bangla.
