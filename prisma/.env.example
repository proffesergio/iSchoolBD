# Neon (Vercel Free Tier) — copy to root .env (never commit real URL)
# Get from: neon.tech Console → Project → Connection string (pooled for serverless)
DATABASE_URL="postgresql://neondb_owner:password@ep-xxx-xxx.ap-southeast-1.aws.neon.tech/ischool-bd?sslmode=require"

# Direct (for prisma migrate, non-pooled):
# DIRECT_URL="postgresql://neondb_owner:password@ep-xxx-xxx.ap-southeast-1.aws.neon.tech/ischool-bd?sslmode=require"

# Existing Supabase Auth (kept for Google/phone login until Neon Auth migration):
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
# NEXT_PUBLIC_API_BASE=http://localhost:3001/api
